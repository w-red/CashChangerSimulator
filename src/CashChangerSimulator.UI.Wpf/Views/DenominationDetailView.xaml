<UserControl x:Class="CashChangerSimulator.UI.Wpf.Views.DenominationDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CashChangerSimulator.UI.Wpf.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             MinWidth="400"
             AutomationProperties.IsOffscreenBehavior="Onscreen"
             d:DesignHeight="350"
             d:DataContext="{d:DesignInstance Type=vm:DenominationViewModel}">
    <Border Background="#1E1E1E" CornerRadius="12" Padding="24" BorderThickness="1" BorderBrush="#33FFFFFF">
        <StackPanel>
            <Grid Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ℹ" Width="20" Height="20" Foreground="#BB86FC" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="16" TextAlignment="Center"/>
                    <TextBlock Text="{Binding LabelInventoryDetail}" FontSize="14" FontWeight="Bold" Foreground="#BB86FC" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Style="{x:Null}" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Padding="0" Width="30" Height="30"
                        Click="CloseButton_Click"
                        AutomationProperties.AutomationId="CloseButton"
                        Background="Transparent" BorderThickness="0" Foreground="#E0E0E0" Cursor="Hand">
                    <TextBlock Text="✕" FontSize="18"/>
                </Button>
            </Grid>
            
            <TextBlock Text="{Binding Name}" FontSize="28" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,24" Foreground="#E0E0E0"/>
            
            <Border Background="#10FFFFFF" CornerRadius="8" Padding="16">
                <StackPanel>
                    <!-- Recyclable -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="↻" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7" Foreground="#E0E0E0" FontSize="16"/>
                            <TextBlock Text="{Binding LabelRecyclable}" VerticalAlignment="Center" FontSize="15" Opacity="0.7" Foreground="#E0E0E0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding RecyclableCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#E0E0E0"/>
                    </Grid>
                    
                    <Separator Background="#20FFFFFF" Margin="0,4" />
                    
                    <!-- Collection -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⬇" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7" Foreground="#E0E0E0" FontSize="16"/>
                            <TextBlock Text="{Binding LabelCollection}" VerticalAlignment="Center" FontSize="15" Opacity="0.7" Foreground="#E0E0E0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CollectionCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#E0E0E0"/>
                    </Grid>
                    
                    <Separator Background="#20FFFFFF" Margin="0,4" />
                    
                    <!-- Reject -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7" Foreground="#E0E0E0" FontSize="16"/>
                            <TextBlock Text="{Binding LabelReject}" VerticalAlignment="Center" FontSize="15" Opacity="0.7" Foreground="#E0E0E0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding RejectCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#E0E0E0"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <Border Margin="0,20,0,0" Padding="16,8" Background="#BB86FC" CornerRadius="8" Opacity="0.1" />
            
            <Grid Margin="16,-38,16,0">
                <TextBlock Text="{Binding LabelTotalCount}" FontSize="12" FontWeight="Black" VerticalAlignment="Center" Opacity="0.6" Foreground="#E0E0E0"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding Count.Value}" FontSize="32" FontWeight="Black" Foreground="#BB86FC" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SuffixCount}" FontSize="14" FontWeight="Bold" Foreground="#BB86FC" VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>

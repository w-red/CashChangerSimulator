<UserControl x:Class="CashChangerSimulator.UI.Wpf.Views.DenominationDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CashChangerSimulator.UI.Wpf.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             MinWidth="400" d:DesignHeight="350"
             d:DataContext="{d:DesignInstance Type=vm:DenominationViewModel}">
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="24" BorderThickness="1" BorderBrush="#33FFFFFF">
        <StackPanel>
            <Grid Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="InformationOutline" Width="20" Height="20" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{StaticResource InventoryDetail}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Padding="0" Width="30" Height="30"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                    <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                </Button>
            </Grid>
            
            <TextBlock Text="{Binding Name}" FontSize="28" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,24" Foreground="{StaticResource OnSurfaceBrush}"/>
            
            <Border Background="#10FFFFFF" CornerRadius="8" Padding="16">
                <StackPanel>
                    <!-- Recyclable -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cached" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7"/>
                            <TextBlock Text="{StaticResource Recyclable}" VerticalAlignment="Center" FontSize="15" Opacity="0.7"/>
                        </StackPanel>
                        <TextBlock Text="{Binding RecyclableCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource OnSurfaceBrush}"/>
                    </Grid>
                    
                    <Separator Background="#20FFFFFF" Margin="0,4" />
                    
                    <!-- Collection -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ArchiveArrowDown" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7"/>
                            <TextBlock Text="{StaticResource Collection}" VerticalAlignment="Center" FontSize="15" Opacity="0.7"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CollectionCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource OnSurfaceBrush}"/>
                    </Grid>
                    
                    <Separator Background="#20FFFFFF" Margin="0,4" />
                    
                    <!-- Reject -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AlertCircleOutline" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7"/>
                            <TextBlock Text="{StaticResource Reject}" VerticalAlignment="Center" FontSize="15" Opacity="0.7"/>
                        </StackPanel>
                        <TextBlock Text="{Binding RejectCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource OnSurfaceBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <Border Margin="0,20,0,0" Padding="16,8" Background="{StaticResource PrimaryBrush}" CornerRadius="8" Opacity="0.1" />
            
            <Grid Margin="16,-38,16,0">
                <TextBlock Text="TOTAL" FontSize="12" FontWeight="Black" VerticalAlignment="Center" Opacity="0.6"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding Count.Value}" FontSize="32" FontWeight="Black" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text="枚" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>

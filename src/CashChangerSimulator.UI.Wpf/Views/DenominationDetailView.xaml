<UserControl x:Class="CashChangerSimulator.UI.Wpf.Views.DenominationDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CashChangerSimulator.UI.Wpf.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             MinWidth="400"
             AutomationProperties.IsOffscreenBehavior="Onscreen"
             AutomationProperties.AutomationId="DenominationDetailDialogView"
             d:DesignHeight="350"
             d:DataContext="{d:DesignInstance Type=vm:DenominationViewModel}">
    <Border AutomationProperties.AutomationId="DenominationDetailDialogViewContent" Background="{DynamicResource MaterialDesign.Brush.SurfaceVariant}" CornerRadius="12" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource MaterialDesign.Brush.Outline}">
        <StackPanel>
            <!-- 診断用の隠しマーカー (UI Automation 検出用) -->
            <Button AutomationProperties.AutomationId="DenominationDetailDialogMark" 
                    Opacity="0.01" Width="1" Height="1" 
                    IsHitTestVisible="False" Focusable="False" />
            <Grid Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{StaticResource SymbolInfo}" Width="20" Height="20" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="16" TextAlignment="Center"/>
                    <TextBlock Text="{Binding LabelInventoryDetail}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Style="{StaticResource HeaderIconButtonStyle}" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"
                        AutomationProperties.AutomationId="CloseButton"
                        Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}">
                    <TextBlock Text="{StaticResource SymbolClose}" FontSize="20"/>
                </Button>
            </Grid>
            
            <TextBlock Text="{Binding Name}" FontSize="28" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,24" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
            
            <Border Background="{DynamicResource MaterialDesign.Brush.Surface}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{DynamicResource MaterialDesign.Brush.Outline}">
                <StackPanel>
                    <!-- Recyclable -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                             <TextBlock Text="{StaticResource SymbolRecycle}" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}" FontSize="16"/>
                            <TextBlock Text="{Binding LabelRecyclable}" VerticalAlignment="Center" FontSize="15" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding RecyclableCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                    </Grid>
                    
                    <Separator Background="{DynamicResource MaterialDesign.Brush.Outline}" Margin="0,4" />
                    
                    <!-- Collection -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                             <TextBlock Text="{StaticResource SymbolCollect}" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}" FontSize="16"/>
                            <TextBlock Text="{Binding LabelCollection}" VerticalAlignment="Center" FontSize="15" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CollectionCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                    </Grid>
                    
                    <Separator Background="{DynamicResource MaterialDesign.Brush.Outline}" Margin="0,4" />
                    
                    <!-- Reject -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                             <TextBlock Text="{StaticResource SymbolWarning}" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}" FontSize="16"/>
                            <TextBlock Text="{Binding LabelReject}" VerticalAlignment="Center" FontSize="15" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding RejectCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                    </Grid>

                    <Separator Background="{DynamicResource MaterialDesign.Brush.Outline}" Margin="0,4" />

                    <!-- Escrow (Deposit Tray) -->
                    <Grid Margin="0,8">
                        <StackPanel Orientation="Horizontal">
                             <TextBlock Text="{StaticResource SymbolHistory}" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}" FontSize="16"/>
                            <TextBlock Text="{Binding LabelEscrow}" VerticalAlignment="Center" FontSize="15" Opacity="0.7" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding EscrowCount.Value}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <Border Margin="0,20,0,0" Padding="16,12" Background="{DynamicResource MaterialDesign.Brush.Surface}" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource MaterialDesign.Brush.Outline}">
                <Grid>
                    <TextBlock Text="{Binding LabelTotalCount}" FontSize="12" FontWeight="Black" VerticalAlignment="Center" Opacity="0.6" Foreground="{DynamicResource MaterialDesign.Brush.OnSurface}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Count.Value}" FontSize="32" FontWeight="Black" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SuffixCount}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Non-recyclable Tip -->
            <materialDesign:Card Padding="12" Margin="0,16,0,0"
                                Background="{DynamicResource MaterialDesign.Brush.SurfaceContainerHigh}"
                                Visibility="{Binding IsRecyclable, Converter={StaticResource InverseBoolToVis}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="InformationOutline" Width="18" Height="18" Margin="0,0,8,0" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
                    <TextBlock Text="{Binding LabelNoteNonRecyclable}" 
                               TextWrapping="Wrap" FontSize="12" Opacity="0.8" VerticalAlignment="Center"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Actions Section -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <!-- Placeholder for action buttons -->
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>

<UserControl x:Class="CashChangerSimulator.UI.Wpf.Views.DispenseIdleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:CashChangerSimulator.UI.Wpf.ViewModels"
             xmlns:conv="clr-namespace:CashChangerSimulator.UI.Wpf.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:DispenseViewModel}"
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConv" />
    </UserControl.Resources>
    <StackPanel>
        <!-- Simple Dispense -->
        <Border Background="#0DFFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="{StaticResource AmountDispense}" FontSize="11" FontWeight="Bold" Opacity="0.6" Margin="0,0,0,12"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" 
                             materialDesign:HintAssist.Hint="{StaticResource AmountHint}"
                             materialDesign:TextFieldAssist.PrefixText="{Binding CurrencyPrefix.Value}"
                             Text="{Binding DispenseAmountInput.Value, UpdateSourceTrigger=PropertyChanged}" 
                             IsEnabled="{Binding CanOperate.Value}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             FontSize="22" FontWeight="Light"
                             AutomationProperties.AutomationId="DispenseBox"/>
                    <TextBlock Grid.Column="0" Text="{Binding CurrencySuffix.Value}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,32,12" FontWeight="Bold" Opacity="0.5" Visibility="{Binding CurrencySuffix.Value, Converter={StaticResource StringNullOrEmptyToVisibilityConv}}"/>

                    <Button Grid.Column="1" 
                            Style="{StaticResource PrimaryButtonStyle}" 
                            Command="{Binding DispenseCommand}" 
                            VerticalAlignment="Center"
                            Padding="0"
                            Margin="12,0,0,0" Height="44" Width="44"
                            AutomationProperties.AutomationId="DispenseButton">
                        <materialDesign:PackIcon Kind="ChevronRight" Width="24" Height="24"/>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>

        <Separator Background="#1AFFFFFF" Margin="0,4,0,16"/>

        <TextBlock Text="{StaticResource QuickDispense}" FontSize="11" FontWeight="Bold" Opacity="0.5" Margin="0,0,0,12" HorizontalAlignment="Center"/>

        <ItemsControl ItemsSource="{Binding Denominations}"
                      ItemsPanel="{StaticResource CenteredWrapPanelItemsPanel}"
                      Margin="0,0,0,16">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Viewbox StretchDirection="DownOnly" Stretch="Uniform" Height="36" Margin="4">
                        <Button Style="{StaticResource SecondaryButtonStyle}"
                                Content="{Binding Name}"
                                Command="{Binding DataContext.QuickDispenseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Width="80" Height="36" FontSize="11" Padding="0"/>
                    </Viewbox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Separator Background="#1AFFFFFF" Margin="0,2,0,10"/>

        <!-- Bulk Dispense (Dialog) -->
        <Button Style="{StaticResource SecondaryButtonStyle}"
                Command="{Binding ShowBulkDispenseCommand}"
                HorizontalAlignment="Stretch" Height="40" Margin="0,0,0,12"
                AutomationProperties.AutomationId="BulkDispenseShowButton">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="PlaylistCheck" Margin="0,0,8,0"/>
                <TextBlock Text="{StaticResource BulkDispense}" FontSize="12" FontWeight="Bold"/>
            </StackPanel>
        </Button>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4">
            <Button Command="{Binding ResetErrorCommand}"
                    Content="{StaticResource ResetError}"
                    AutomationProperties.AutomationId="ResetErrorButton"
                    FontSize="10" Padding="8,2"
                    Margin="0,0,8,0">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource DangerButtonStyle}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsJammed.Value}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsOverlapped.Value}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsDeviceError.Value}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Style="{StaticResource SecondaryButtonStyle}" 
                    Command="{Binding SimulateJamCommand}"
                    Content="{StaticResource SimulateJam}"
                    AutomationProperties.AutomationId="SimulateJamButton"
                    FontSize="10" Padding="8,2"/>
        </StackPanel>
    </StackPanel>
</UserControl>

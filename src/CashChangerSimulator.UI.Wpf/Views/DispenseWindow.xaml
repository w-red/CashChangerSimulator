<Window x:Class="CashChangerSimulator.UI.Wpf.Views.DispenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:CashChangerSimulator.UI.Wpf.ViewModels"
        xmlns:views="clr-namespace:CashChangerSimulator.UI.Wpf.Views"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:DispenseViewModel}"
        Title="DISPENSE TERMINAL" Height="650" Width="420"
        WindowStartupLocation="CenterOwner"
        AutomationProperties.AutomationId="DispenseWindow"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource OnBackgroundBrush}"
        TextElement.Foreground="{StaticResource OnBackgroundBrush}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="16"
        ResizeMode="CanResize" MinWidth="360" MinHeight="450">

    <materialDesign:DialogHost Identifier="DispenseDialog" Style="{StaticResource MaterialDesignEmbeddedDialogHost}">
        <Border Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="{StaticResource DispenseTerminal}" 
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontWeight="Black"/>
                </StackPanel>

                <!-- Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ContentControl Content="{Binding}">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="ContentTemplate" Value="{StaticResource DispenseIdleTemplate}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StatusName.Value}" Value="Busy">
                                        <Setter Property="ContentTemplate" Value="{StaticResource DispenseBusyTemplate}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StatusName.Value}" Value="Error">
                                        <Setter Property="ContentTemplate" Value="{StaticResource DispenseErrorTemplate}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </materialDesign:DialogHost>
</Window>

<Window x:Class="CashChangerSimulator.UI.Wpf.Views.DispenseWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:CashChangerSimulator.UI.Wpf.ViewModels"
        xmlns:views="clr-namespace:CashChangerSimulator.UI.Wpf.Views"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:DispenseViewModel}"
        Title="{StaticResource DispenseTerminal}" Height="650" Width="420"
        WindowStartupLocation="CenterOwner"
        AutomationProperties.AutomationId="DispenseWindow"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        Foreground="{DynamicResource MaterialDesign.Brush.OnBackground}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.OnBackground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="16"
        ResizeMode="CanResize" MinWidth="360" MinHeight="450">

    <materialDesign:DialogHost Identifier="DispenseDialog" Style="{StaticResource MaterialDesignEmbeddedDialogHost}">
        <Border Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="{StaticResource DispenseTerminal}" 
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                               Foreground="{DynamicResource MaterialDesign.Brush.OnSurfaceVariant}"
                               FontWeight="Black"/>
                </StackPanel>

                <!-- Content Switching via Visibility (Fixed for UI Automation Stability) -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <views:DispenseIdleView AutomationProperties.AutomationId="DispenseIdleView">
                            <views:DispenseIdleView.Style>
                                <Style TargetType="UserControl">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentState.Value}" Value="{x:Static vm:DispenseUIState.Idle}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </views:DispenseIdleView.Style>
                        </views:DispenseIdleView>

                        <views:DispenseBusyView AutomationProperties.AutomationId="DispenseBusyView">
                            <views:DispenseBusyView.Style>
                                <Style TargetType="UserControl">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentState.Value}" Value="{x:Static vm:DispenseUIState.Busy}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </views:DispenseBusyView.Style>
                        </views:DispenseBusyView>

                        <views:DispenseErrorView AutomationProperties.AutomationId="DispenseErrorView">
                            <views:DispenseErrorView.Style>
                                <Style TargetType="UserControl">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentState.Value}" Value="{x:Static vm:DispenseUIState.Error}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </views:DispenseErrorView.Style>
                        </views:DispenseErrorView>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Border>
    </materialDesign:DialogHost>
</Window>
